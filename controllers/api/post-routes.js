const router = require('express').Router();
const { Post, Comment, User } = require('../../models');

// need:
// post route
// delete route
// get all
// get by id
// put route

router.get('/', (req, res) => {

})

module.exports = router;